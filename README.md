# Kagi

コミットグラフを中心とした安全な Git GUI クライアント。Rust + GPUI で構築しています。

## ビルド前提

- Rust stable (rustup でインストール)
- macOS
- full Xcode は不要 (Xcode Command Line Tools のみで可)

## 起動方法

```sh
cargo run
```

初回ビルドは gpui の依存解決で数分かかります。
