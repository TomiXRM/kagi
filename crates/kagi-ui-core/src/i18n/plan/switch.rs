//! JA strings for `SwitchNote` (filled by the ops/switch fan-out PR — ADR-0129 Phase 2).

use kagi_domain::plan_note::SwitchNote;

/// Japanese rendering of one switch note.
pub fn note_ja(note: &SwitchNote) -> String {
    match *note {}
}
