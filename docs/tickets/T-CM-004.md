# T-CM-004: Menu item model + availability 純関数

- Status: done
- 依存: ADR-0021
- 関連: docs/requirements-context-menu.md / ADR-0020〜0026

## スコープ

1. `MenuContext` / `MenuGroup` / `MenuItem` / `ItemState{Enabled,Disabled(reason),Hidden}` /
   `CommitAction` enum を `src/ui/context_menu.rs` に定義(ADR-0020/0021/0022 の形)
2. `build_commit_menu(ctx: &MenuContext) -> Vec<MenuGroup>` を純関数で実装
   (正準表 = requirements-context-menu.md の availability 表)
3. `MenuContext` 構築ヘルパー `KagiApp::menu_context(row_index)`(graph_descendant_of 1回まで)
4. headless: `KAGI_CONTEXT_MENU=<row>` で全項目と enabled/disabled(理由) をログ出力

## 完了条件

- [ ] 6状況(HEAD/過去/別branch/merge/dirty/detached)の unit test が通る(T-CM-063 の先行分)
- [ ] headless ログで出し分けが確認できる
- [ ] `cargo test` 全パス + own-code warning 0(コード変更がある場合)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/ui/context_menu.rs`(新規)/ `src/ui/mod.rs`(最小限)/ `src/main.rs`(env 経路)
- `docs/tickets/T-CM-004.md`

## 触ってはいけないファイル

- 上記以外(特に `scripts/*` / `Cargo.toml` / 他チケットのファイル)

## テスト方法

1. `cargo test`(パイプで握りつぶさず exit code を確認)
2. `bash scripts/make_fixture.sh` の fixture / tempdir のみで検証(ユーザー repo 禁止)
3. UI 変更は PM がスクリーンショット確認(headless ログを併設すること)
## リスク・規約

- 表とコードの乖離。表を正とし、テスト名に表の行を対応させる
- force push / reset --hard / git clean をコードに追加するのは絶対禁止(ADR-0024 の隔離例外を除き、本チケットでは一切不可)
- 文字列切り詰めは chars() ベース(byte slice は日本語で panic)

## 実装メモ

- `MenuContext` / `MenuGroup` / `MenuItem` / `ItemState` / `CommitAction` と `build_commit_menu` を `src/ui/context_menu.rs` に追加。
- `KagiApp::menu_context(row_index)` は row/detail/status/badges と `graph_descendant_of(HEAD, target)` から availability を構築する。
- `KAGI_CONTEXT_MENU=<row>` で `[kagi] context-menu: row=N items=...` を出力する。
