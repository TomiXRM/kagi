# T-CM-023: Create worktree here の plan + 実行

- Status: done
- 依存: ADR-0025, T-CM-024
- 関連: docs/requirements-context-menu.md / ADR-0020〜0026

## スコープ

1. `src/git/ops.rs` に `plan_create_worktree(repo, branch, path, start) -> OperationPlan` と
   `execute_create_worktree`(git2 `Repository::worktree`、branch 新規作成とセット)
2. blockers: path 既存 / repo 内 path / 親 dir 不在 / branch 名衝突。recovery: `git worktree remove <path>`
3. dialog(branch 名 + path 入力、default `../<repo>-worktrees/<branch>`)
4. 実行後 Navigator に WORKTREES セクション(`repo.worktrees()` 列挙、reload で更新)
5. headless: `KAGI_PLAN_WORKTREE=<branch>:<path>` + AUTO_CONFIRM、`[kagi] sidebar: ... worktrees=N`
6. tests/worktree_test.rs(新規 suite): 成功 / path 衝突 / branch 衝突 / preflight 不一致

## 完了条件

- [ ] plan 経由で worktree 作成、Navigator に表示
- [ ] 新 suite 全パス
- [ ] worktree 内 repo を kagi で開けることを fixture で確認
- [ ] `cargo test` 全パス + own-code warning 0(コード変更がある場合)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/git/ops.rs` / `src/git/mod.rs`(re-export)/ `src/ui/`(dialog+sidebar)/ `src/main.rs` / `tests/worktree_test.rs`(新規)
- `docs/tickets/T-CM-023.md`

## 触ってはいけないファイル

- 上記以外(特に `scripts/*` / `Cargo.toml` / 他チケットのファイル)

## テスト方法

1. `cargo test`(パイプで握りつぶさず exit code を確認)
2. `bash scripts/make_fixture.sh` の fixture / tempdir のみで検証(ユーザー repo 禁止)
3. UI 変更は PM がスクリーンショット確認(headless ログを併設すること)
## リスク・規約

- git2 worktree API の挙動差(bare/locked)。fixture で実挙動を先に確認してから実装
- force push / reset --hard / git clean をコードに追加するのは絶対禁止(ADR-0024 の隔離例外を除き、本チケットでは一切不可)
- 文字列切り詰めは chars() ベース(byte slice は日本語で panic)

## 実装メモ

- `plan_create_worktree` / `execute_create_worktree` を追加し、`git2::Repository::worktree` + 新規 branch 作成で実装。
- `CommitAction::CreateWorktreeHere` を worktree dialog に接続。dialog は branch/path 入力、default path は `../<repo>-worktrees/<branch>`。
- `RepoSnapshot` に worktrees を追加し、Navigator に `WORKTREES` section と headless `worktrees=N` log を追加。
- `KAGI_PLAN_WORKTREE=<branch>:<path>` + `KAGI_AUTO_CONFIRM=1` の headless 経路を追加。
- `tests/worktree_test.rs` を追加し、成功 / path 衝突 / branch 衝突 / preflight 不一致 / 日本語 path validation を確認。
