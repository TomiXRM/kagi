# T-CM-030: Cherry-pick availability 判定

- Status: done
- 依存: T-CM-004
- 関連: docs/requirements-context-menu.md / ADR-0020〜0026

## スコープ

1. build_commit_menu 内: HEAD 選択 → disabled「HEAD と同一」/ HEAD から到達可能 →
   disabled「既に現在 branch に含まれています」/ merge → disabled「merge commit は未対応(MVP)」/
   detached → disabled「detached HEAD」/ dirty → enabled + plan で warning
2. 判定 unit test(T-CM-063 に含めてもよい)

## 完了条件

- [ ] 上記 5 ケースの unit test
- [ ] `cargo test` 全パス + own-code warning 0(コード変更がある場合)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/ui/context_menu.rs`
- `docs/tickets/T-CM-030.md`

## 触ってはいけないファイル

- 上記以外(特に `scripts/*` / `Cargo.toml` / 他チケットのファイル)

## テスト方法

1. `cargo test`(パイプで握りつぶさず exit code を確認)
2. `bash scripts/make_fixture.sh` の fixture / tempdir のみで検証(ユーザー repo 禁止)
3. UI 変更は PM がスクリーンショット確認(headless ログを併設すること)
## リスク・規約

- graph_descendant_of の向き(descendant_of(HEAD, target) = target が祖先)を間違えない。既存 undo_commit の使い方を参照
- force push / reset --hard / git clean をコードに追加するのは絶対禁止(ADR-0024 の隔離例外を除き、本チケットでは一切不可)
- 文字列切り詰めは chars() ベース(byte slice は日本語で panic)

## 実装メモ

- `build_commit_menu` の cherry-pick availability 文言を「既に現在 branch に含まれています」に統一。
- context menu unit test で HEAD / current-branch ancestor / merge / detached / dirty の availability を確認。
- 検証: `cargo test`、fixture headless 既存ログ確認。
