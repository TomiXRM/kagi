# T-CM-032: Cherry-pick conflict handling の確認・補強

- Status: todo
- 依存: T-CM-031
- 関連: docs/requirements-context-menu.md / ADR-0020〜0026

## スコープ

1. 既存 in-memory 方式の conflict 予測(blocker で Refused、repo 無傷)が menu 経路でも
   機能することを fixture で確認
2. Refused 時の toast(W3-NOTIFY)+ Operation Log 記録を確認
3. 不足があれば conflict ファイル一覧を blockers 文面に追加

## 完了条件

- [ ] conflict fixture で Refused + repo 無傷 + toast/oplog 確認
- [ ] `cargo test` 全パス + own-code warning 0(コード変更がある場合)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/git/ops.rs`(文面のみ)/ tests(必要なら)
- `docs/tickets/T-CM-032.md`

## 触ってはいけないファイル

- 上記以外(特に `scripts/*` / `Cargo.toml` / 他チケットのファイル)

## テスト方法

1. `cargo test`(パイプで握りつぶさず exit code を確認)
2. `bash scripts/make_fixture.sh` の fixture / tempdir のみで検証(ユーザー repo 禁止)
3. UI 変更は PM がスクリーンショット確認(headless ログを併設すること)
## リスク・規約

- 「conflict 状態の表示」は in-memory 方式では発生しない(repo に conflict を作らない)。要件の意図は Refused 表示で満たす — 文書化すること
- force push / reset --hard / git clean をコードに追加するのは絶対禁止(ADR-0024 の隔離例外を除き、本チケットでは一切不可)
- 文字列切り詰めは chars() ベース(byte slice は日本語で panic)
