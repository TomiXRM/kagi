# T-CM-062: Inspector Actions と Context Menu の handler 統合

- Status: todo
- 依存: T-CM-010, T-CM-021, T-CM-031
- 関連: docs/requirements-context-menu.md / ADR-0020〜0026

## スコープ

1. Inspector の Actions(Create branch / Cherry-pick / Copy SHA)を全て
   `dispatch_commit_action` 経由に置換(直接実装を削除)
2. Inspector 側の availability 表示にも MenuContext を使う(ADR-0021)

## 完了条件

- [ ] Inspector に直接実装が残っていない(grep で確認)
- [ ] 既存 Inspector 操作に回帰なし
- [ ] `cargo test` 全パス + own-code warning 0(コード変更がある場合)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/ui/inspector.rs` / `src/ui/mod.rs` / `src/ui/context_menu.rs`
- `docs/tickets/T-CM-062.md`

## 触ってはいけないファイル

- 上記以外(特に `scripts/*` / `Cargo.toml` / 他チケットのファイル)

## テスト方法

1. `cargo test`(パイプで握りつぶさず exit code を確認)
2. `bash scripts/make_fixture.sh` の fixture / tempdir のみで検証(ユーザー repo 禁止)
3. UI 変更は PM がスクリーンショット確認(headless ログを併設すること)
## リスク・規約

- Inspector の既存ログ文字列を変えない(headless 互換)
- force push / reset --hard / git clean をコードに追加するのは絶対禁止(ADR-0024 の隔離例外を除き、本チケットでは一切不可)
- 文字列切り詰めは chars() ベース(byte slice は日本語で panic)
