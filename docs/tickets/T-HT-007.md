# T-HT-007: Stash Pop backend + Stash plan 拡張(T-HT-006 統合)

- Status: in-progress(worktree 並行)
- 依存: T015(ADR-0009: pop は「conflict 予測時 blocker + apply 提案」)
- 範囲: **backend + テストのみ**(UI 配線は PM が main 側で実施)

## スコープ

### 1. Stash plan 拡張(T-HT-006 分)

- `plan_stash_push` に **対象ファイル一覧**を入れる(warnings ではなく説明行として。
  既存フィールドで表現: 一覧文字列を warnings に `"will stash: N files"` + 先頭数件、または
  preview 用の既存 Vec フィールドがあればそれを使う — worktree 時点の OperationPlan 定義に従う)
- untracked を含めるかのフラグ: `plan_stash_push(repo, message, include_untracked: bool)` /
  `execute_stash_push(.., include_untracked)` に拡張(既存呼び出しは true 固定で互換維持。
  **既存テストは変更禁止** — シグネチャ変更で壊れる場合のみ呼び出し側の最小修正可)

### 2. Stash Pop(ADR-0009 の Destructive 緩和付き)

- `pub fn plan_stash_pop(repo: &mut Repository, index: usize) -> Result<OperationPlan, GitError>`
  - blocker: index 範囲外 / conflict 状態 / **dirty WT**(apply と同じポリシー)/
    **conflict 予測あり**(stash commit と HEAD の in-memory merge(`merge_commits` 等)で判定。
    conflict 予測時は「pop すると stash が消えるため拒否。apply を使ってください」と明記)
  - 表示: 「pop = apply + drop(成功時に stash が消える)」の説明 / 対象 stash の message
- `pub fn execute_stash_pop(repo: &mut Repository, index: usize) -> Result<(), GitError>`
  - `repo.stash_apply(index, None)` → 成功時のみ `repo.stash_drop(index)`
  - apply が失敗したら drop しない(stash 無傷)
  - ※ stash_drop は ADR-0004 で Destructive だが、ADR-0009 で「pop の一部としてのみ」許可された経緯を
    doc comment に明記。**単独の stash_drop API は公開しない**(pub にしない)
- テスト(`tests/stash_pop_test.rs` 新規):
  - pop 正常系: 変更が戻り stash が消える(count-1)
  - conflict 予測 → blocker + stash 残存 + repo 無傷
  - dirty → blocker / 範囲外 → blocker
  - apply 失敗時に drop されない(構成が難しければ conflict 予測 blocker で代替し、その旨メモ)
  - push の include_untracked=false で untracked が残る
  - 計 6+ ケース、tempdir のみ

## 完了条件

- [ ] `cargo test` 全パス(このブランチ内)+ 警告 0
- [ ] 単独の `stash_drop` が public API に存在しない(grep)
- [ ] 実装メモを本ファイル末尾に追記
- [ ] **worktree の branch に「T-HT-007: stash pop backend」としてコミット**(PM が merge する)

## 触ってよいファイル

- `src/git/`(ops.rs / staging 周りではなく stash 関連 / mod.rs)
- `tests/stash_pop_test.rs`(新規)。既存テストはシグネチャ追従の最小修正のみ可(理由をメモ)
- `docs/tickets/T-HT-007.md`

## 触ってはいけないファイル

- `src/ui/` / `src/main.rs`(UI は PM)/ その他すべて
