# T004: commit log を取得して内部モデルに変換する

- Status: in-progress
- 担当: Sonnet subagent
- 依存: T002

## 背景

commit graph(本プロダクトの中心機能)の入力データとなる commit DAG を取得する。
T006 の graph layout はこの出力(topo order の `Vec<Commit>`)をそのまま入力に取るため、
**順序の保証(子が親より先)と parents の正確さ**が品質のすべて。

## スコープ

1. ドメインモデル(architecture.md §3)を `src/git/` に導入:
   - `CommitId(String)` newtype(40-hex。`short()` ヘルパー(先頭8桁)付き)
   - `Signature { name: String, email: String, time: i64 }`(epoch 秒。表示整形は UI 側)
   - `Commit { id: CommitId, parents: Vec<CommitId>, author: Signature, committer: Signature, summary: String, message: String }`
   - parents[0] が first parent であることを保証(doc comment に明記)
2. backend に log 取得を実装(例: `src/git/log.rs`):
   - `pub fn commit_log(repo: &Repository, limit: usize) -> Result<Vec<Commit>, GitError>`
   - revwalk で**全 ref から到達可能な commit**を列挙: `push_glob("refs/heads/*")` + `push_glob("refs/remotes/*")` + `push_glob("refs/tags/*")` + HEAD(detached 対応のため `push_head()` も。unborn は空 Vec を返す)
   - ソート: `Sort::TOPOLOGICAL | Sort::TIME`(新しい順、**子が必ず親より先**)
   - `limit` で打ち切り(呼び出し側 MVP 値: 10,000)
   - 非 UTF-8 の author / message は lossy 変換で受ける(panic しない)
3. UI: 表示は最小限でよい。「Commits: N」と先頭3件の `short id + summary` を既存の details 表示に追加(リスト UI は T008)
4. integration test(`tests/log_test.rs`):
   - tempdir + git CLI で「分岐 + merge を含む repo」を組み立て、以下を assert:
     - 全 commit が取得される(ref から到達可能なもの)
     - merge commit の parents が2つで、parents[0] が first parent(`git log --pretty=%P` と突き合わせ)
     - topo order: すべての commit について、その親はリスト内でより後ろに位置する
     - limit が効く
     - summary / message の取得(複数行 message で summary が1行目になる)
   - 既存 tests/status_test.rs のヘルパー(git() 等)は重複定義してよい(共通化リファクタは禁止。tests/common/ を作るのは可)

## 完了条件

- [ ] `cargo check` / `cargo build` / `cargo test` が通る(既存6テスト含む)
- [ ] 新規テストで topo order / first parent / limit / summary を検証
- [ ] fixture repo で「Commits: 9」が表示される(merge 含む。`git rev-list --count --branches --remotes --tags` = 9 と一致。stash の内部 commit(--all だと 11)は含まれないこと)
- [ ] unborn repo でクラッシュせず「Commits: 0」
- [ ] 公開 API に doc comment
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/main.rs`(表示追加のみ)
- `src/git/`(新規モジュール追加と mod.rs への組み込み)
- `tests/`(新規テストファイル追加。既存テストの変更は禁止)
- `docs/tickets/T004.md`(実装メモ追記)

## 触ってはいけないファイル

- `docs/architecture.md` / `docs/requirements.md` / `docs/adr/*`
- `docs/tickets/INDEX.md` および他の ticket
- `scripts/make_fixture.sh`(使用可・変更禁止)
- `Cargo.toml` / `Cargo.lock`(依存追加は不要のはず。必要なら理由を報告して中断)
- `README.md` / `tests/status_test.rs` / `src/git/status.rs`

## テスト方法

1. `cargo test`
2. `bash scripts/make_fixture.sh` → `cargo run -- <repoパス>`(バックグラウンド起動 + sleep + kill 方式)で stderr ログを確認
3. 検証は fixture / tempdir の repo のみ

## リスク

- stash は `refs/stash` にいるため glob に含めなければ出ない(意図どおり。stash 表示は T015/T005 で別扱い)
- `Sort::TOPOLOGICAL` だけだと同世代の順序が不定 → `| Sort::TIME` で安定化。ただし git CLI の `--topo-order` と完全一致はしなくてよい(「子が親より先」だけが不変条件)
- annotated tag の peel: `push_glob("refs/tags/*")` は revwalk が自動で commit まで peel する(エラーになる場合は報告)

## 実装メモ

### 実装日: 2026-06-12 / 担当: Sonnet subagent

#### 追加ファイル

- `src/git/log.rs` — `CommitId` / `Signature` / `Commit` モデルと `commit_log()` 実装
- `tests/log_test.rs` — 7 件の integration test

#### 変更ファイル

- `src/git/mod.rs` — `mod log;` と `pub use log::{Commit, CommitId, Signature, commit_log};` 追加
- `src/main.rs` — `commit_log` を使い「Commits: N + 先頭3件」を stderr/details に追加

#### 実装上のポイント

**unborn repo の判定**: `repo.is_empty()` は libgit2 の内部ロジック(init.defaultbranch と HEAD の symbolic target を `strcmp` で比較)が macOS デフォルト `master` に対して `git init -b main` で作ったリポジトリを `false` 扱いする問題がある。代わりに `repo.head()` の `ErrorCode::UnbornBranch` を使うことで確実に判定できる。

**push_glob の NotFound**: unborn repo では `push_glob("refs/heads/*")` が `Ok(())` を返すが、その後 `push_head()` が `NotFound`(libgit2 は `UnbornBranch` の代わりに `GIT_ENOTFOUND` を返す場合がある)を返す。そのため `push_head()` のエラーハンドリングで `NotFound` も許可し、revwalk の iterator でも `NotFound` を break として扱う。

**parent の順序保証**: `git2::Commit::parent_ids()` は libgit2 が返す順序(first parent = 0)をそのまま維持するため、明示的なソートは不要。

**fixtures の Commits: 9**: `stash` は `refs/stash` にあり `refs/heads/*` / `refs/remotes/*` / `refs/tags/*` の glob に含まれないため、stash 内部 commit(2件)が含まれず正しく9件になる。

#### テスト構成(7件)

| テスト名 | 検証内容 |
|---|---|
| `test_unborn_repo_is_empty` | unborn repo で空 Vec を返す(クラッシュしない) |
| `test_all_commits_returned` | 分岐+merge の5件が全取得される |
| `test_topological_order` | すべての commit について親が後ろに位置する |
| `test_merge_commit_parents` | parents[0] が `git log --pretty=%P` の first parent と一致 |
| `test_limit` | limit=3/1/0 が正確に機能する |
| `test_summary_and_multiline_message` | summary=1行目、message に2段落以降が含まれる |
| `test_all_refs_included` | HEAD 以外の branch(orphan branch)から到達できる commit も含まれる |
