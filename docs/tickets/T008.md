# T008: GPUI で commit list を表示する(仮想化リスト)

- Status: in-progress
- 担当: Sonnet subagent
- 依存: T001, T005

## 背景

これまでの UI は stderr ログ + テキスト羅列だった。本チケットで Graph Panel の土台となる
**仮想化された commit リスト**を作る(非機能要件: 10k commits でもスクロールが軽い)。
lane 描画は T009、行選択は T010 で重ねるため、ここでは「リストが出る」ことに集中する。

## スコープ

1. UI モジュール構成の導入:
   - `src/ui/mod.rs` + `src/ui/commit_list.rs`(バイナリ側 `main.rs` から `mod ui;` で参照。lib.rs に入れない: テスト対象は domain/backend のみ)
   - `KagiApp`(root view)が `RepoSnapshot` を保持し、commit list を子要素として描画
2. commit list(`uniform_list` を使用):
   - **gpui 0.2.2 の `uniform_list` API を docs.rs で必ず確認してから実装**(シグネチャを想像で書かない)
   - 各行: `short id`(等幅・薄色)/ summary(主テキスト、1行 truncate)/ author 名 / 日付
   - 日付は epoch 秒から自前整形でよい(「3d ago」「2y ago」程度の相対表記。**新規依存 crate は追加しない**。SystemTime::now との差分で計算)
   - 行高は固定(uniform_list の前提)
3. ref バッジ:
   - snapshot から `CommitId → Vec<RefBadge>` map を作るヘルパーを `src/ui/` に実装
   - badge: HEAD(強調色)/ local branch / remote branch / tag を色違いの小さなチップで commit 行に表示
   - HEAD が attached の場合、その branch チップに HEAD マークを統合してよい(`main ✓` 等)
4. レイアウト: ウィンドウ全体を縦分割 — 上部に repo 名 + HEAD + status 概要のヘッダ(1行)、残り全部が commit list
5. エラー/引数なし表示は従来どおり(クラッシュしない)
6. stderr ログ([kagi] 行)は検証手段として維持する(リスト行数のログ等)

## 完了条件

- [ ] `cargo check` / `cargo build` / `cargo test`(既存38)が通る
- [ ] fixture repo で 9 commits がリスト表示され、`main`(HEAD)/ `feature/*` / `origin/*` / `v0.1.0` のバッジが該当行に付く
- [ ] 大量 commit(後述のスクリプトで 1000 commits の tempdir repo を作って)でも起動・表示できる
- [ ] unborn repo / エラーケースでクラッシュしない
- [ ] 実装メモ(使った gpui API と注意点)を本ファイル末尾に追記

## 触ってよいファイル

- `src/main.rs` / `src/ui/`(新規)
- `docs/tickets/T008.md`(実装メモ追記)
- 検証用の一時スクリプトは作らず、シェルワンライナーで tempdir に 1000 commits repo を作ってよい(tempdir 限定)

## 触ってはいけないファイル

- `src/git/` / `src/graph/` / `src/lib.rs`(ui は bin 側に置くので lib 変更不要のはず。必要なら理由を報告)
- `docs/` の他ファイル、`Cargo.toml` / `Cargo.lock`(依存追加禁止)、`tests/*`、`scripts/*`

## テスト方法

1. `cargo test`(既存38が壊れないこと)
2. fixture repo: `bash scripts/make_fixture.sh` → 起動(バックグラウンド + sleep 5 + kill -0 + kill)→ 生存 + [kagi] ログでリスト行数 9 を確認
3. 1000 commits repo(例: tempdir で `for i in $(seq 1000); do echo $i > f; git add f; git commit -qm "c$i"; done`)→ 起動して生存確認
4. 検証は fixture / tempdir のみ

## リスク

- gpui 0.2.2 の `uniform_list` のシグネチャ・クロージャ引数は版によって違う。**docs.rs/gpui/0.2.2 が唯一の正**(git main の examples を見ない)
- uniform_list は要素の事前 measure をしない前提なので行内レイアウトを固定高に収めること
- SharedString への変換コスト(毎フレーム format! しない。snapshot 取得時に表示用文字列を組み立てて保持する)

## 実装メモ

(subagent が追記する)
