# T011: changed files list を表示する

- Status: in-progress
- 担当: Sonnet subagent
- 依存: T010

## 背景

requirements M-05 の後半。「commit を選択すると metadata と **changed files** が見える」を完成させる。
ここで作る diff バックエンドの土台(first-parent との tree diff)は T012(file diff viewer)でそのまま拡張する。

## スコープ

1. backend(`src/git/diff.rs` 新規、lib 側):
   - `pub fn commit_changed_files(repo: &Repository, id: &CommitId) -> Result<Vec<FileStatus>, GitError>`
   - 比較対象: **commit の tree vs first parent の tree**(root commit は empty tree と比較)
   - `diff_tree_to_tree` + rename 検出(`diff.find_similar(&mut DiffFindOptions::new().renames(true))`)
   - delta の status → 既存 `ChangeKind`(Added / Modified / Deleted / Renamed{from} / TypeChange)に変換。
     その他の delta status(Copied 等)は Modified に丸めてよい(メモに書く)
   - 既存の `FileStatus` / `ChangeKind`(status.rs)を再利用。新型を作らない
2. integration test(`tests/diff_test.rs`):
   - tempdir + git CLI で組み立て、以下を assert:
     - added / modified / deleted / renamed(`git mv` 相当 + 類似度検出)の各 1 ファイル
     - root commit(全ファイル Added)
     - merge commit(first parent との diff であること: second parent 由来の変更が一覧に出る)
3. UI(Detail Panel 拡張):
   - `KagiApp` に `repo_path: Option<PathBuf>` を保持し、**選択時にオンデマンドで** `open_repository` 相当 + `commit_changed_files` を呼ぶ(全 commit 分の事前計算は禁止: 10k commits で遅すぎる)
   - 結果は `HashMap<usize, Vec<...>>` 等でキャッシュ(同じ commit の再選択で再計算しない)
   - パネルの Message の下に `Changed files (N)` セクション: 各行は change kind の1文字バッジ(A=green / M=yellow / D=red / R=blue / T=gray)+ パス(長い場合は先頭を `…` で省略してよい)
   - 表示は最大 100 件で truncate し、超過分は `… and N more` と出す
   - diff 取得に失敗してもクラッシュせず `（diff unavailable）` 表示
   - 選択時ログに `[kagi] changed files: N` を追加(headless 検証用。`KAGI_SELECT_FIRST=1` で出ること)
4. UI 側の取得は同期で可(MVP。バックグラウンド化は T017 以降の課題としてメモ)

## 完了条件

- [ ] `cargo check` / `cargo build` / `cargo test`(既存48 + 新規)が通る
- [ ] fixture repo + `KAGI_SELECT_FIRST=1` で `changed files: 1`(行0 = "add c.txt (unpushed)" → c.txt 1件)がログに出る
- [ ] merge commit 選択で first-parent diff のファイルが出る(テストで担保)
- [ ] root commit 選択でクラッシュしない(全 Added)
- [ ] 公開 API に doc comment
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/git/`(diff.rs 新規 + mod.rs への組み込みのみ)
- `src/ui/` / `src/main.rs`
- `tests/diff_test.rs`(新規)
- `docs/tickets/T011.md`(実装メモ追記)

## 触ってはいけないファイル

- `src/graph/` / `src/lib.rs` / 既存 `tests/*` / `scripts/*`
- `docs/` の他ファイル、`Cargo.toml` / `Cargo.lock`

## テスト方法

1. `cargo test`
2. fixture + `KAGI_SELECT_FIRST=1` 起動(バックグラウンド + sleep 5 + kill -0 + kill)→ 生存 + `selected:` / `changed files:` ログ
3. 検証は fixture / tempdir のみ

## リスク

- rename 検出は `find_similar` を呼ばないと Added+Deleted に分かれる(テストの renamed ケースで検出可能)
- root commit の empty tree 比較: `diff_tree_to_tree(None, Some(&tree), ...)` で `old_tree=None` を渡せる
- merge commit の diff は「first parent と比較」が GUI の通例(git show と同じ)。combined diff はやらない
- 選択のたびに Repository::open するコスト: MVP では許容(ローカル SSD で ms オーダー)。気になる場合もリファクタしない(T017 でワーカー化を検討)

## 実装メモ

### 変更ファイル一覧

- `src/git/diff.rs` — 新規: `commit_changed_files()` 実装
- `src/git/mod.rs` — `mod diff;` / `pub use diff::commit_changed_files;` 追加
- `src/ui/mod.rs` — `KagiApp` に `repo_path` / `diff_cache` 追加、`select()` でオンデマンド取得、`render_detail_panel()` に Changed files セクション追加
- `src/main.rs` — `app_state.repo_path = Some(repo_path.clone());` 追加
- `tests/diff_test.rs` — 新規: 6テスト (root / added / modified / deleted / renamed / merge)

### 使用した git2 API

```rust
// diff 生成 (old_tree=None で empty tree との比較になる)
repo.diff_tree_to_tree(
    old_tree: Option<&Tree<'_>>,
    new_tree: Option<&Tree<'_>>,
    opts: Option<&mut DiffOptions>,
) -> Result<Diff<'_>, Error>

// rename 検出
let mut find_opts = DiffFindOptions::new();
find_opts.renames(true);
diff.find_similar(Some(&mut find_opts)) -> Result<(), Error>

// delta 列挙
diff.deltas() -> impl Iterator<Item = DiffDelta<'_>>

// delta フィールド
delta.status() -> Delta   // Added/Deleted/Modified/Renamed/Typechange/Copied 等
delta.old_file().path() -> Option<&Path>
delta.new_file().path() -> Option<&Path>
```

### Delta → ChangeKind マッピング

| git2::Delta  | ChangeKind           | 備考                                    |
|-------------|----------------------|-----------------------------------------|
| Added       | Added                |                                         |
| Deleted     | Deleted              |                                         |
| Modified    | Modified             |                                         |
| Renamed     | Renamed { from }     | find_similar 後のみ出現。old_file().path() が元パス |
| Typechange  | TypeChange           |                                         |
| Copied 等   | Modified             | 保守的フォールバック(Copied = Added+history とも言えるが Modified で統一) |

### キャッシュ設計

`diff_cache: HashMap<usize, Option<Vec<FileStatus>>>`
- キー: row index
- 値: `Some(Vec)` = 成功、`None` = 失敗 (diff unavailable 表示)
- 同じ commit の再選択では再計算しない

### UI 取得フロー(同期・MVP)

選択 → `select()` → `diff_cache` に未登録なら `fetch_changed_files()` →
`Repository::open(repo_path)` + `commit_changed_files()` → キャッシュ保存 →
render 時に `diff_cache.get(index)` → `render_detail_panel(d, files)` へ渡す

非同期化は T017 課題として先送り。ローカル SSD で ms オーダーのため MVP 許容。

### 完了条件チェックリスト

- [x] `cargo check` / `cargo build` / `cargo test` (既存48 + 新規6 = 54テスト通過)
- [x] fixture + `KAGI_SELECT_FIRST=1` で `changed files: 1` ログ出力確認
- [x] merge commit 選択で first-parent diff (test_merge_commit_first_parent_only で担保)
- [x] root commit でクラッシュしない (test_root_commit_all_added で担保)
- [x] 公開 API に doc comment (diff.rs モジュールコメント + 関数コメント)
- [x] 実装メモを本ファイル末尾に追記

### 未解決リスク

- 差分取得は同期 (select ごとに Repository::open)。ネットワーク越しマウントや大規模 pack では遅延が出る可能性あり → T017 でワーカー化検討
- `Diff` の lifetime は `repo` の lifetime に束縛されるため、`repo` を `KagiApp` に持たせる設計は gpui の `'static` 制約と相性が悪い。そのため `select()` の中でローカルに `Repository::open` している
