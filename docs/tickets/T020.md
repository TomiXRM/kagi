# T020: グラフエッジの直角(角R)化 + コミッターアバター表示(ユーザー要望)

- Status: in-progress
- 担当: Sonnet subagent
- 依存: T009, T008
- 発端: ユーザー要望「ツリーの分岐を斜め直線ではなく直角(角はR)にしてほしい。GitKraken みたいなコミッターのアイコンが欲しい」

## スコープ

### 1. グラフエッジの直角 + 丸角化(`src/ui/graph_view.rs`)

現在: IntoNode / OutOfNode の lane またぎが**斜めの直線**。
変更後: **縦線 → 丸角コーナー → 横線**(GitKraken / gitk 風)。

- `CORNER_R ≈ 6.0`(px)。lane 間隔や行高に対して大きすぎる場合は `min(CORNER_R, |dx|/2, 利用可能な縦距離/2)` にクランプ
- **IntoNode**(from_lane j → node lane、j ≠ node):
  `(x_j, 0)` から縦に `(x_j, mid_y - R)` → 丸角 → 横線 `(x_node ± R, mid_y)` → node 中心へ
- **OutOfNode**(node lane → to_lane j、j ≠ node):
  node 中心から横線 `(x_j ∓ R, mid_y)` → 丸角 → `(x_j, mid_y + R)` から縦に `(x_j, row_h)`
- from == to のケース(直進)は従来どおり縦線
- 丸角は PathBuilder の bezier 系 API(`curve_to` / `cubic_bezier_to` 等。
  **registry の examples/painting.rs と src/path_builder.rs で実 API を確認**)。
  bezier がなければ小さい line セグメント数本での近似でも可(メモに明記)
- 線幅・色規則(IntoNode=from色 / OutOfNode=to色)は変更しない

### 2. コミッターアバター(イニシャル方式、ネットワーク不要)

- ヘルパー(`src/ui/avatar.rs` 新規、pure + unit test):
  - `avatar_color(email: &str) -> Hsla` — email の安定ハッシュ(FNV-1a 等の自前実装で可)から
    パレット(彩度・明度固定、hue を 12 分割程度)を選ぶ。同じ email は常に同じ色
  - `avatar_initial(name: &str) -> String` — 名前の先頭1文字を大文字化(chars ベース。日本語もそのまま1文字)。
    空なら "?"
  - unit test: 同一 email の安定性 / 異なる email で色が分散 / 日本語名 / 空文字
- 表示(`src/ui/mod.rs` の commit 行):
  - graph 領域と SHA の間に **直径 18px の丸**(`div().rounded_full().bg(色)`)+ 中央に白イニシャル(`text_sm`)
  - author の name/email は `CommitRow` 構築時に取得済みのものを使う(なければ commit_list.rs に追加)
  - 行高 24px は維持
- Detail Panel の Author 行の先頭にも同じアバター(任意・小さく)
- Gravatar 等のネットワーク取得は **later**(README 等には書かない。ticket メモに将来案として記す)

## 完了条件

- [ ] `cargo test`(既存 + avatar unit tests)が通る
- [ ] fixture で起動し、merge / 分岐の lane またぎが「縦 → 丸角 → 横」で描画される(PM がスクリーンショット確認)
- [ ] 各 commit 行にイニシャルアバターが表示され、同一 author は同色
- [ ] 1000 commits repo でスクロール起動に問題なし(アバターは div ベースなので canvas 追加なし)
- [ ] 既存 headless 検証に回帰なし
- [ ] 実装メモを本ファイル末尾に追記

## 触ってよいファイル

- `src/ui/`(graph_view.rs, mod.rs, commit_list.rs, avatar.rs 新規)
- `docs/tickets/T020.md`(実装メモ追記)

## 触ってはいけないファイル

- `src/git/` / `src/graph/`(layout のデータは変えない。描画だけの変更)/ `src/lib.rs` / `src/main.rs` / `tests/*` / `scripts/*`
- `docs/` の他ファイル、`Cargo.toml` / `Cargo.lock`

## テスト方法

1. `cargo test`
2. fixture(merge あり)で起動・目視(スクリーンショットは PM)
3. 直線 repo / 1000 commits repo で回帰なし
4. 検証は fixture / tempdir のみ

## リスク

- 行高 24px に対して corner R=6 は上下それぞれ mid までの 12px に収まる(R クランプを必ず実装)
- bezier API のシグネチャ違い → 必ず registry ソースで確認。なければ折れ線近似
- 丸角の向き(左折/右折)の場合分けミス → fixture の merge(右から左へ合流)と分岐(左から右)の両方向を目視確認
- イニシャルの絵文字対応: chars().next() で十分(grapheme cluster までは不要、メモに明記)

## 実装メモ

(subagent が追記する)
