# W22-I18N: UI 多言語対応 wave 1(en / ja)

- Status: in-progress
- 担当: worktree agent(Opus)
- 仕様の正: **ADR-0048**(必読。対象/非対象の表に厳密に従う)

## スコープ

1. `src/ui/i18n.rs`(新規): Lang / lang() / set_lang / Msg enum + `t()`(ADR-0048 の形)。
   引数つき文は i18n 側のヘルパ fn。`KAGI_LANG` override + `LANG`/`LC_ALL` 既定判定
2. **src/ui/ の説明文を Msg 化**: モーダル(checkout/stash/pop/amend/discard/delete-branch/
   cherry-pick/revert/create-branch/create-worktree/commit plan/smart-commit consent 注記)、
   トースト、Busy footer、WIP 行の注記、空状態文、tooltip 説明文、既存の日本語ハードコード。
   **ドメインワード・ボタン単語・列ヘッダは英語のまま**(ADR の表)
3. メニュー View → Language(English / 日本語、✓、テーマ切替 `set_theme` と同型の `set_lang`)
4. settings.json `"lang"` 永続化(theme の read_setting/write_setting と同じ機構)
5. ADR-0044 の同意 4 文言は **verbatim 英文を維持**(変更禁止)
6. `src/git/` の文字列は**触らない**(wave 2)

## 触ってよいファイル

- `src/ui/i18n.rs`(新規)/ `src/ui/*.rs`(文字列の Msg 化と menu/lang 配線)
- `tests/i18n_test.rs`(新規: KAGI_LANG 切替で Msg::t が変わる unit test 数本)
- 本チケット

## 触ってはいけないファイル

- `src/git/**`(blocker/recovery 文言はテストが固定。wave 2)/ `Cargo.toml` / `vendor/`

## 検証

- `KAGI_LANG=ja` / `=en` で起動し、モーダル説明文が切り替わる(PM がスクリーンショット)
- 既存テスト全パス(文言を参照するテストを壊さない — UI 層の文言で test が固定しているものが
  あれば KAGI_LANG=en を test 内で明示)+ own-code warning 0
- 文字列処理は chars() ベース。**バイトスライス禁止(split_at 含む)**

## 共通規約

- fixture / tempdir のみ。完了時: 実装メモ + Status: done、worktree branch に commit(push/merge しない)
