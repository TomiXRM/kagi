//! Markdown preview for the Editor workspace — moved to
//! `kagi-ui-editor::markdown` (ADR-0121 C4); re-exported here so every
//! existing `crate::ui::editor_markdown::…` path keeps working.

pub use kagi_ui_editor::markdown::*;
